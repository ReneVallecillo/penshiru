#.travis.yml

language: node_js
node_js:
  - "6.10"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli
  - ng build --prod

script:
  - ng test --watch=false

notifications:
  email:
    on_failure: reneval@gmail.com
    on_success: reneval@gmail.com